## [Unreleased]


## 0.5.0 - 2026-09-12

### 新增

- `series.webui@2.0` 新增环境设置面板，统一维护常驻地点、主动关心字段、缓存与风险阈值，并提供受控的天气、空气质量和日历连通性探测。

### 修复

- 兼容实际 `UsageTracker` 的私有路径字段，使 `series.control` 能正确读写同一份运行目录状态文件。

## 0.4.0 - 2026-09-12

### 新增

- 接入 `series.module@1.0` 与 `series.webui@2.0`，环境状态面板可继续由独立 `pages/status` 或核统一接管访问。

### 修复

- 修正模块契约中误写的 JSON 布尔字面量，并同步版本一致性回归测试。

## 0.3.2 - 2026-09-12

### 新增

- 新增只读 `series.webui@1.0` 状态面板：核独立 WebUI 可查看默认地点、主动关心、机会缓存与后台刷新状态；现有独立 `pages/status` 继续保留。

## 0.3.1 - 2026-08-21

### 新增

- 接入 `series.control@1.0`，允许“核”统一管理环境关心候选的安全运行开关；关闭接管后回退插件原生配置。

## 0.3.0 - 2026-08-19

- 发布状态页可访问性、Bridge 恢复与动效优化。

- 状态页补齐面板可访问语义、触屏 hover 与 reduced-motion 降级，并在 Bridge 初始化超时时显示可重试错误。

## 0.2.3 - 2026-08-07

- 系列诊断提供方补齐统一自声明字段，可由“核”动态发现并独立清空。
- 修正 `get_weather` 的主模型调用语义：直接询问当前温度、天气、是否下雨或未来天气属于显式实时事实请求，不再要求必须同时存在穿衣、通勤或出行决策。
- 明确“你那 / 你那里 / 你那边”的地点规则：仅单用户且有效常驻地点已配置时允许留空调用；地点归属不明时先自然追问，不猜测地点、不编造天气，也不等待用户自行报告。
- 保留非调用边界：文学比喻、虚构、回忆、纯情绪化天气表达和仅提到出门上班不会触发；仍由当前主模型一次判断，不增加关键词路由或额外 LLM 请求。
- 新增直接温度与降雨问法、非现实天气反例、地点指代规则和缺少地点安全降级的回归测试。

## 0.2.2 - 2026-08-05

### 变更

- 同步系列诊断提供方契约，新增插件无需修改“核”即可被日志页发现。

## 0.2.1 - 2026-08-03

### 变更

- 修复 Plugin Page 在 Bridge 未加载时静默失败的问题，增加中文错误提示和顶部配置读取失败提示。
- 页签补齐 ARIA 关联与方向键/Home/End 操作；下拉项优先显示中文。
- 数字配置校验改为严格提示具体字段，不再静默回退默认值；同步页面缓存参数。
- 完善系列诊断日志：捕获范围扩展到 `DEBUG`-`CRITICAL`，单插件内存缓冲上限为 1000 条；自动捕获事件现在保留最长 2000 字符的脱敏日志正文，供“核”点击展开，且继续不读取 AstrBot 全局日志。

## 0.2.0 - 2026-07-31

### 头像

- 更新凝心溯溪-境 的插件头像为本次发布的 `logo.png`。

### 新增

- 新增 `series.diagnostics@1.0` 系列诊断日志接口：只在内存中保留有限数量的启动状态、关键运行节点和异常告警，供“核”的日志页统一读取；未安装或未运行“核”时不影响本插件正常工作。

### 调整

- 补充市场短简介，并把外显说明统一为凝心溯溪系列环境模块的一句话描述。

### 安全

- 诊断日志与 AstrBot 主日志隔离，写入前会脱敏敏感标识并截断过长内容，不保存聊天正文、令牌或异常堆栈；日志不落盘，插件重载或 AstrBot 重启后自动清空。
- 自动告警仅暴露结构化定位信息，不保存格式化后的日志正文；清空会轮换诊断流标识，避免新旧实例事件混合。

## 0.1.4 - 2026-07-30

### 调整

- Plugin Page 统一为“情”“核”使用的系列视觉基线，并按“总览 / 配置 / 实时检查”分区；保留原有地点设置、调用统计、参数编辑和数据源探测能力。

### 测试

- 增加管理页三页签结构与切换脚本的静态回归断言。

## 0.1.3 - 2026-07-30

### 修复与调整

- 移除无法控制 AstrBot 静态 Plugin Page 入口可见性的 `page_enabled` 开关，管理页改为常驻可用，不再显示“页面未启用”的占位状态。
- 修复 `广东省珠海市` 等完整中文行政区名称无法解析：原始查询无结果时按行政层级逐级回退，并用父级省市信息过滤同名地点。
- 修复 `珠海` 被 Open-Meteo 优先解析到山东同名小地点：低人口普通居民点会继续尝试城市后缀；`杭州` 等已正确返回的行政中心不会增加额外请求。

### 测试

- 新增完整行政区回退、父级匹配、同名小地点纠正和常驻页面接口回归测试。

## 0.1.2 - 2026-07-30

### 新增

- 管理页新增显式 `page_enabled` 开关，默认关闭；关闭时除最小状态外，配置、地点保存和数据源探测接口均拒绝访问。
- 管理页新增基于配置 schema 的分组参数编辑与热应用，数值范围在前后端同时约束；常驻地点继续要求先解析校验再保存。
- 新增跨重启调用统计，覆盖手动命令、LLM 工具、实际发生的轻量感知与主动交付；展示累计次数、入口分布和最近 12 次调用。
- 新增浏览器设备定位按钮，仅在用户点击并授权后获取坐标，随后直接校验并保存为常驻地点。

### 隐私与测试

- 调用记录只保存时间、入口、功能、结果与耗时，不保存消息、用户标识、UMO 或地点；页面探测不计入真实调用次数。
- 新增页面关闭保护、配置范围校验与热应用、统计持久化和隐私字段、定位控件及页面配置契约测试。

## 0.1.1 - 2026-07-30

### 变更

- 新增 `environment.opportunity@1.0` 后台环境关心候选：只从新鲜的官方预警、高 AQI/UV、强天气、相关地震和强降温中选择一条结构化事实；普通回复钩子只读缓存，不同步联网，也不要求模型必须提及。
- 新增真正主动环境关心的完整安全门：默认关闭，要求配置自然人和完整私聊 UMO，并由序确认精确收件白名单、情确认账号归属、言决定是否发送及最终措辞；缺少任一依赖时不降级直发。
- 新增主动消息暂停、当地安静时段、严重度门槛、跨重启事件去重、仅严重度升级可重发和当地日期每日上限。境不解析 UID、不读取原始关系分数，也不直接调用平台发送接口。
- 模型已判断不发送或输出被安全门拒绝的同一事件会按严重度记忆，不在每次后台刷新时重复请求模型；严重度升级后仍可重新判断，且未发送的判断不占每日发送额度。
- 环境候选后台刷新与普通回复解耦；候选包含稳定事件键与事实修订哈希，低级或过期候选不会启动新关心。数据源刷新失败时仍保留显式查询原有的旧缓存降级语义。
- 单个来源失败或只返回旧数据时，不会用其它来源的“暂无事件”误清该来源仍在有效期内的高严重度候选；旧候选也不会压过其它来源的新鲜更高严重度事实。
- 关闭候选缓存且未开启主动发送后，后台循环会自行退出；切换常驻地点会立即失效旧地点候选并重新刷新。回复钩子不再以发送者 UID 作为 UMO 缺失时的降级标识。
- 公共日历轻量感知结果按当地日期与配置缓存，不再在同日每轮重复构造节假日日历。
- 对齐凝心溯溪-核的 `plugin.health@1.0`：更新后可执行 HTTP 会话与六工具注册的 L1 业务健康检查，可选常驻地点未配置不再混入健康失败条件。
- 扩展六个 LLM 工具的语义调用说明：覆盖跨时区联系、作息、约见、出行、通勤、穿衣带伞、晾晒、户外运动、开窗、防晒、过敏暴露与附近震感等现实决策，仍由当前主模型自主判断，不增加关键词路由或额外 LLM 请求。
- 为天气、空气质量和环境风险补充最小够用查询范围、只使用当前决定相关数据、非医疗结论和不渲染恐慌边界；关心语气与篇幅继续服从当前人格、关系及对话约束。明确仅提到出门上班、纯寒暄、历史文化、文学虚构、影视游戏、只描述症状和无地点新闻不应触发。
- 地点列表工具改为只用于管理员配置排查；环境查询直接调用目标工具，避免为确认地点配置多一次工具往返。
- 明确 `default_location` 是不按用户隔离的插件级全局地点：仅单用户部署允许工具留空使用，多用户部署必须显式传入地点，地点列表不能充当身份识别手段。
- 重要节假日轻量事实明确为公共日历而非用户个人安排；只允许在确有帮助时作一句简短问候或调休关心，不相关时忽略，不单独播报、连续追问或转移当前话题。

### 测试

- 新增候选结构与隐私字段、AQI/UV/降温阈值、稳定事件键与修订、来源优先级、跨午夜安静时段、严重度升级去重、模型抑制去重、部分来源失败保留、关闭后台、旧候选不注入和协同预检回归测试。
- 新增工具语义场景、非关键词路由和重要日自然参考边界的提示契约测试。

## 0.1.0 - 2026-07-29

### 新增

- 初步实现凝心溯溪-境。
- 新增免 API Key 的 Open-Meteo 地点解析、当地时间、当前天气、逐小时和逐日预报。
- 新增 USGS 地震数据接入与地点相关性过滤。
- 新增本地强降水、阵风、极端温度和雷暴模型信号。
- 新增四个按需 LLM 工具、三条手动查询命令和插件快速设置页。
- 新增公共假日、周末、调休工作日与下一个节假日查询，并支持重要日当地当天首次轻量感知。
- 新增 Open-Meteo AQI、污染物、紫外线与可用花粉数据。
- 新增日出、日落、白昼长度、能见度及未来约 6 小时的 15 分钟降水模型数据。
- 新增中央气象台当前官方气象预警，按省查询后再按城市、区县和省级发布机构过滤。
- LLM 工具由四个扩展为六个，手动命令新增 `/境日历` 与 `/境空气`，天气命令新增 `nowcast` 范围。
- 新增 TTL 缓存、并发请求合并、旧缓存标记回退和数据源诊断。

### 安全与边界

- 普通对话不发起网络请求，不注入常驻环境提示，不启动主动推送。
- 超出震级影响半径或管理员绝对距离的事件不会进入模型上下文。
- 数据源失败不会被解释成“当前没有风险”。
- 中央气象台官方预警与 Open-Meteo 模型风险信号采用独立字段和来源标签。
- 同省其它城市、过期、未来时间异常或结构不完整的预警不会进入模型上下文。
- 重要日感知只使用已校验地点的离线缓存，不在普通 LLM 请求中发起环境网络查询。
- 工具公共结果不暴露常驻地点经纬度。

### 文档与测试

- README、配置 schema、数据源说明、限制和开发验证同步建立。
- 新增节假日、空气质量、短临降水、日照与官方预警的单元测试和页面诊断字段。
- 完成首个正式版本 `0.1.0` 的文档、版本元数据与 MIT 许可证。
- 状态页补齐面板可访问语义、触屏 hover 与 reduced-motion 降级，并在 Bridge 初始化超时时显示可重试错误。
